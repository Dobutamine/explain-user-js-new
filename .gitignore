node_modules

# Vite build output (regenerated by `npm run build`)
dist

# env files (bot URL + API key etc.) — never commit; keep *.example templates tracked
.env
.env.*
!.env.example
!.env.local.example

# macOS Finder + Obsidian editor cruft
.DS_Store
.obsidian/
